services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: velkor
      POSTGRES_PASSWORD: velkor_dev_password
      POSTGRES_DB: velkor_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
